import { z } from 'zod'
import { Add{{ pascalCase (singular name) }}FormSchema } from '@/schemas/{{ camelCase name }}Schemas'

export type Add{{ pascalCase (singular name) }}FormFields = z.infer<typeof Add{{ pascalCase (singular name) }}FormSchema>
