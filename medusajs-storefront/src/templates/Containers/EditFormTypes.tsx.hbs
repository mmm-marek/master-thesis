import { z } from 'zod'
import { Edit{{ pascalCase (singular name) }}FormSchema } from '@/schemas/{{ camelCase name }}Schemas'

export type Edit{{ pascalCase (singular name) }}FormFields = z.infer<typeof Edit{{ pascalCase (singular name) }}FormSchema>
