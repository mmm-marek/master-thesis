import { useTranslations } from 'next-intl'
import { useForm, UseFormReset } from 'react-hook-form'

import InputField from '@/atoms/InputField/InputField'
import Button from '@/atoms/Button/Button'
import HookFormField from '@/components/HookFormField'
import { zodResolver } from '@/utils/zodResolver'
import { Add{{ pascalCase (singular name) }}FormFields } from '@/containers/{{ camelCase name }}/components/Add{{ pascalCase (singular name) }}Form/Add{{ pascalCase (singular name) }}FormTypes'
import { Add{{ pascalCase (singular name) }}FormSchema } from '@/schemas/{{ camelCase name }}Schemas'
import * as SC from './Add{{pascalCase (singular name)}}FormStyles'

type Props = {
	onSubmit: (data: Add{{ pascalCase (singular name) }}FormFields, reset: UseFormReset<Add{{ pascalCase (singular name) }}FormFields>) => Promise<void>
	initValues?: Add{{ pascalCase (singular name) }}FormFields
}

const Add{{ pascalCase (singular name) }}Form = (props: Props) => {
	const { onSubmit, initValues } = props
	const t = useTranslations('components.add{{ pascalCase (singular name) }}Form')

	const {
		control,
		reset,
		formState: { isSubmitting },
		handleSubmit
	} = useForm<Add{{ pascalCase (singular name) }}FormFields>({
		mode: 'onChange',
		resolver: zodResolver(Add{{ pascalCase (singular name) }}FormSchema),
		defaultValues: { name: '' },
		values: initValues
	})

	const handleFormSubmit = async (data: Add{{ pascalCase (singular name) }}FormFields) => {
		await onSubmit({ ...data }, reset)
	}

	return (
		<SC.Form onSubmitCapture={handleSubmit(handleFormSubmit)}>
			<HookFormField
				label={t('nameLabel')}
				placeholder={t('namePlaceholder')}
				component={InputField}
				control={control}
				name={'name'}
				size={'large'}
				required
			/>
			<Button type={'primary'} size={'large'} htmlType='submit' block disabled={isSubmitting} loading={isSubmitting}>
				{t('submitButton')}
			</Button>
		</SC.Form>
	)
}

export default Add{{ pascalCase (singular name) }}Form
