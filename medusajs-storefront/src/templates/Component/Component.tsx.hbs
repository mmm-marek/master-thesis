import { useTranslations } from 'next-intl'
import * as SC from './{{pascalCase name}}Styles'

type Props = {}

const {{pascalCase name}} = (props: Props) => {
	const t = useTranslations('components.{{camelCase name}}')
	return <SC.{{pascalCase name}}Wrapper>
		{t('testText')}
	</SC.{{pascalCase name}}Wrapper>
}

export default {{ pascalCase name }}
