import * as z from 'zod'
import { VALIDATION_MAX_LENGTH } from '@/utils/enums'
import { stringConstraint } from '@/utils/globalZod'
import { PaginationSchema } from '@/schemas/globalSchemas'

export const Add{{ pascalCase (singular name) }}FormSchema = z.object({
	name: stringConstraint(VALIDATION_MAX_LENGTH.LENGTH_255, true)
})

export const Edit{{ pascalCase (singular name) }}FormSchema = z.object({
	name: stringConstraint(VALIDATION_MAX_LENGTH.LENGTH_255, true)
})

export const {{ pascalCase (singular name) }}QuerySchema = z.object({
	{{ camelCase (singular name) }}Id: z.string().optional()
})

export const {{ pascalCase name }}QuerySchema = PaginationSchema.extend({
	// Other params such as filters, sorting, etc
})
